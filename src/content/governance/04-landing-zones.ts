import type { Section } from '../types'

export const landingZones: Section = {
  id: 'landing-zones',
  title: 'Landing zones',
  scene: 'the-shape-of-an-estate',
  slide: `## A shape, not a product

A **landing zone** is a subscription that arrives already correct: policy applied, network attached, logging wired, roles assigned. Nobody builds those four things per project.

### The split that does the work
- **Platform** — shared, built once, owned by one team: *Identity*, *Connectivity* (the hub VNet and firewall), *Management* (the one workspace)
- **Landing zones** — where applications go. *Corp* for internal, *Online* for public-facing, differing mainly in what the network allows

### The two groups everyone omits
**Sandbox** — no baseline, no peering, so experiments have somewhere legal to happen instead of happening in prod. **Decommissioned** — cancelled subscriptions, kept because the audit trail outlives the project.

### Where the baseline goes
At **Tenant Root**, once. Everything below inherits it, and an exception is an explicit exclusion you can point at.

> The subscription is the unit. Make a new one cheap and correct, and people stop crowding into yours.`,
  narration:
    "Landing zone is a phrase that sounds like a product you buy, and it is not. It is a shape, and I want to define it in one sentence before we look at it. A landing zone is a subscription that arrives already correct — the policy baseline is applied, the network is attached to the hub, diagnostic logging is going to the right workspace, and the right roles are assigned to the right groups. That is it. The value is entirely in the word already. Because the alternative, which is what most organisations do first, is that every project sets those four things up for itself. And four things times fifteen projects is sixty decisions made by people who each have something else they would rather be doing, and the result is fifteen slightly different estates that nobody can reason about collectively. Now look at the shape on the left, because the important thing about it is one split. Underneath the tenant root there are two big management groups, and the difference between them is who owns what is inside. Platform is the shared stuff, built once, owned by one team. Three subscriptions live there conventionally. Identity, which holds directory-adjacent things and domain services if you have them. Connectivity, which holds the hub virtual network, the firewall, the gateways — everything network-shaped that more than one application depends on. And Management, which holds the one Log Analytics workspace that everything logs to, plus your automation and your backup vaults. Landing zones is the other one, and that is where applications actually go. The convention is two flavours. Corp is for internal workloads — they get connectivity back to the hub, they can reach on-premises, and they accept no public inbound traffic. Online is for public-facing workloads, which need less connectivity backward and more scrutiny at the edge. They differ mainly in what the network policy allows, and honestly, if you only ever have one of these, have Corp. Now the two management groups that everybody leaves off the first version and then needs within a quarter. Sandbox is a group with no policy baseline and no network peering. That sounds like a hole in your governance and it is the opposite: it is where experiments go. If there is nowhere legal to try something without the baseline, people will try it in a subscription that has the baseline, and quietly ask for an exception, and the exception will become permanent. Give them a sandbox with no connectivity to anything real, and the problem stops existing. Decommissioned is for subscriptions belonging to projects that have ended. You cannot just delete a subscription — there is usually an audit obligation, or data retention, or simply a bill somebody has to reconcile — so they get moved to a management group whose policy denies creating anything new. Which brings us to the baseline itself, and where it goes. At tenant root, assigned once. Everything below inherits it, which means a new subscription created next March is compliant the moment it exists, without anybody remembering to do anything. And when a workload genuinely needs an exception, you exclude that scope from that assignment explicitly, which leaves the exception visible, attributable, and reviewable — rather than invisible, which is what you get when each subscription is configured individually. The last thing I want to leave you with is the economic argument, because it is the one that persuades people. In Azure a subscription is free. It is a billing and policy boundary, not a thing you pay for. So the moment a new subscription is cheap to create and arrives correct, the incentive to crowd four unrelated workloads into one subscription — which is where most governance problems actually come from — disappears entirely.",
}
