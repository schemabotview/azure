import type { Section } from '../types'

export const securityReview: Section = {
  id: 'security-review',
  title: 'The security review',
  scene: 'the-checklist-against-this-design',
  slide: `## The checklist, answered honestly

Eight ticks, one *not applicable*, and one **declined**. The last two are what make it a review rather than a list.

### Not applicable, recorded
**Defender for Servers** protects VMs. There are none — the compute is Container Apps and the data services are managed. Writing *not applicable, because nothing here is a VM* stops the question being re-asked every quarter by someone reading a generic standard.

### Declined, with a reason and a date
**Sentinel** is the right product and this is the wrong moment. Two engineers, no rota, so incidents queue unread — which is a cost **and** a liability, because the log then shows an alert fired and nobody moved. Revisit when there is somebody to page.

### The one the design nearly failed
The PSP's API key cannot be a managed identity — it is a third party. It lives in **Key Vault**, read by the API's identity, rotated quarterly. **One secret in the whole system**, and it is the one we could not remove.

> A review is where you write down what you chose not to do, and why.`,
  narration:
    "This is the security review, and the format is the checklist from the security course walked against this specific design. Eight items pass, and I am not going to read you eight ticks — they are on screen and each one traces to a decision an earlier section already explained. What is worth your time is the last two lines, because those are what make this a review rather than a list, and because a page of ticks is exactly what a review looks like when nobody thought hard. The first is Defender for Servers, marked not applicable. And the reason is concrete: Defender for Servers puts endpoint detection on virtual machines, and there are no virtual machines in this design. The compute is Container Apps, the database is managed, the storage is managed. There is no operating system we patch. Now, the important part is not the conclusion, it is that it is written down with the reason attached. Because otherwise, every quarter, somebody reads a generic security standard that says enable endpoint protection on all servers, sees an unticked box, and raises it. And each time, somebody has to reconstruct the reasoning from scratch and defend it. Written down once, with a reason, it stops being a recurring cost. The second is Sentinel, and this one is a genuine declination rather than a non-applicability, and I want to make the argument properly because it is the kind of judgment that separates a design from a compliance exercise. Sentinel is the right product. Everything we said about it in the security course holds — a SIEM turns a query into an incident, and incidents with playbooks are how you respond to things at three in the morning. And it is the wrong moment for this team. Two engineers, no on-call rota. If we deploy Sentinel, incidents are raised into a queue that nobody is on the other end of. That is a cost, because Sentinel bills by the gigabyte ingested and we would be connecting sources to feed it. And it is worse than a cost, it is a liability — because you have now created a documented record showing that an alert fired and no human responded. In the event that something actually happens, that record is materially worse than not having had the alert. So it is declined, the reason is written down, and there is a date attached: revisit when there is somebody to page. If the team goes to five people with a rota, the answer changes, and whoever is here then will find the reasoning rather than an absence. Now let me talk about the item the design nearly failed on, because it is the interesting one. Throughout this course I have been fairly triumphant about managed identity and about removing credentials. And there is one credential in this system that cannot be removed: the payment provider's API key. They are a third party. They do not federate with Entra. They issue a key and you send it with every request, and no amount of architecture makes that go away. So: it lives in Key Vault. It is read at startup by the API's managed identity, which has Secrets User on that vault and nothing else. It is rotated quarterly, with the two-version overlap that lets the fleet catch up. And every read of it is a line in the vault's audit log, which is the thing we actually wanted. That is one secret in the entire system. Not zero — one, and it is the one we could not remove, and we know exactly where it is and who read it last. That is a genuinely defensible position, and it is very different from the usual situation, which is that nobody knows how many secrets there are. If there is one thing to take from this section it is the framing. A review is not the part where you confirm you did the good things. It is the part where you write down what you chose not to do, why, and what would change your mind. The eight ticks took ten minutes. The two lines underneath took the rest of the meeting, and they are the ones worth having.",
}
