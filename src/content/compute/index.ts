import type { Course } from '../types'

// Declared from day one so the whole arc is visible in the catalog; fills as its slice is authored.
// The section plot is in COURSE-PLAN.md.
export const compute: Course = {
  id: 'compute',
  title: 'VMs to AKS',
  sections: [],
}
